import { PageHeader } from "@/components/admin/page-header";

export default function AiUploadsPlaceholderPage() {
  return (
    <div>
      <PageHeader
        title="Uploads"
        subtitle="Manual asset ingestion lands in V1.1 — use AI Studio + Storage for MVP."
      />
    </div>
  );
}
